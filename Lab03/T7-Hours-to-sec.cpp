#include<iostream>
using namespace std;
int main(){
    float hour;
    cout<<"Enter the Hours = ";
    cin>>hour;

    cout<<hour<<" is equivalent to "<<hour*60*60<<" seconds";
    cin.get();
    return 0;
}